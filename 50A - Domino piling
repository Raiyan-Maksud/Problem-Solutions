#include<bits/stdc++.h>
using namespace std;
int main(){
    int m,n, cnt=0;
    cin>>m>>n;
    int mul=m*n;

    for (int i = 0; i < ((m*n)/2); i++)
    {
        mul=mul-2;
        cnt++;
    }
    cout<<cnt<<endl;
    return 0;
}
